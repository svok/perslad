# **Personal Local Assistant for Developer (PersLAD)**

## Архитектурная спецификация (v3 — финальная для MVP)

---

## 1. Цель системы

PersLAD — это **локальный интеллектуальный помощник разработчика**, обеспечивающий:

* устойчивое владение контекстом проекта (код + документация),
* корректную работу с изменениями,
* минимизацию галлюцинаций,
* воспроизводимость решений.

> Система **не является источником истины** и **не заменяет разработчика**.
> Она — **инструмент анализа, понимания и сопровождения изменений**.

---

## 2. Ключевые принципы (неизменяемые)

1. **Код и документация — источник истины**
2. **Генерация ≠ знание**
3. **Понимание всегда предшествует изменению**
4. **Неуверенность — допустима и должна быть явной**
5. **Эволюция через seed’ы, а не через Big Design Up Front**
6. **MVP прежде всего**

---

## 3. Основной сценарий (core flow)

1. Пользователь задаёт запрос
2. Система:

    * извлекает релевантный контекст (RAG),
    * формирует **Understanding State**
3. Выполняется:

    * анализ,
    * выявление конфликтов,
    * оценка риска
4. При необходимости:

    * уточнение,
    * либо эскалация
5. Генерируются **предложения**, а не изменения
6. Пользователь принимает решение
7. Результат фиксируется в памяти

---

## 4. Архитектура верхнего уровня

```
┌─────────────┐
│   User      │
└─────┬───────┘
      ↓
┌─────────────┐
│ Interaction │
│   Layer     │
└─────┬───────┘
      ↓
┌──────────────────────────┐
│ Context & Reasoning Core │
│  - RAG                   │
│  - Understanding State   │
│  - Claims                │
│  - Risk Assessment       │
└─────┬────────────────────┘
      ↓
┌──────────────────┐
│ Knowledge Layer  │
│  - Code Index    │
│  - Docs Index    │
│  - Dialogue Mem  │
└─────┬────────────┘
      ↓
┌──────────────────┐
│ Execution Layer  │
│  - Proposal Gen  │
│  - Escalation    │
│  - Lineage       │
└──────────────────┘
```

---

## 5. Источники знаний (MVP)

### 5.1. Code Knowledge

* Исходный код
* AST / symbols (опционально)
* Структурная навигация

### 5.2. Documentation Knowledge

* Markdown / README / ADR
* Комментарии
* Wiki (если локально)

### 5.3. Dialogue Memory (принято, MVP)

**Назначение:**
Сохранять **контекст решений**, а не просто чат.

**Важно:**
❌ не источник истины
✅ источник **проектной памяти**

**Хранится:**

* запрос пользователя,
* использованный проектный контекст,
* предложения системы,
* решение пользователя.

```json
{
  "query": "...",
  "project_id": "...",
  "sources_used": ["chunk_12", "file_auth.py"],
  "proposals": ["proposal_42"],
  "decision": "accepted | rejected | modified",
  "timestamp": "..."
}
```

Индексируется семантически, **с понижающимся временным весом**.

---

## 6. Модель знаний

### 6.1. Claim

Минимальная единица знания.

```json
{
  "statement": "AuthService использует JWT",
  "source": "code | doc | inferred",
  "confidence": 0.8,
  "freshness": "commit_hash | timestamp"
}
```

> ❌ completeness_score — **отклонено для MVP**
> Причина: сложно измерить, не влияет на core flow

---

### 6.2. Claims extraction

Используется для:

* выявления противоречий,
* оценки риска,
* объяснения пользователю.

Fallback при сбое → простой RAG.

---

### 6.3. Dialogue Memory

(см. 5.3 — вынесено осознанно как first-class элемент)

---

## 7. Understanding State (ядро системы)

**Understanding State** — это **снимок текущего понимания системы по конкретному запросу**.

```json
{
  "task_goal": "...",
  "relevant_files": [...],
  "claims": [...],
  "unresolved_conflicts": [...],
  "confidence": 0.0-1.0,
  "change_radius": "local | medium | wide",
  "risk_assessment": {...}
}
```

> ❌ Project Context как отдельный persistent-объект — **отклонён**
>
> **Причина:**
>
> * усложняет MVP
> * дублируется Dialogue Memory + Knowledge Index
> * риск ложной консистентности

📌 Вместо этого:
**Understanding State последнего принятого решения = implicit project context**

---

## 8. Оценка риска (принято)

```json
{
  "breaking_changes": [
    {"file": "auth.py", "probability": 0.6}
  ],
  "test_coverage": 0.4,
  "rollback_complexity": "medium",
  "documentation_impact": "high"
}
```

Используется для:

* выбора уровня автоматизации,
* эскалации,
* объяснения пользователю.

---

## 9. Работа с неопределённостью (новый раздел)

### 9.1. Типы неопределённости

1. **Эпистемическая** — нет фактов
2. **Семантическая** — непонятен смысл
3. **Процедурная** — неясно, как делать

---

### 9.2. Политика уверенности

| Confidence | Поведение                         |
| ---------- | --------------------------------- |
| ≥ 0.7      | Можно предлагать изменения        |
| 0.3 – 0.7  | Только предложения + пояснения    |
| < 0.3      | Запрос верификации, без генерации |

---

## 10. Эскалация (уточнено)

### 10.1. Когда эскалируем

1. `unresolved_conflicts ≥ 3` **и** `confidence < 0.6`
2. `change_radius == wide` **и** `risk == high`
3. Явный флаг пользователя `/deep`

### 10.2. Обязательное объяснение

> «Эскалирую, потому что обнаружено 4 противоречия между кодом и документацией»

---

## 11. Модель изменений

### 11.1. Уровни изменений

| Уровень | Поведение            |
| ------- | -------------------- |
| Local   | Автогенерация        |
| Medium  | Diff + подтверждение |
| Wide    | Только рекомендации  |

---

### 11.2. Lineage (обязательно)

Каждое предложение имеет:

* proposal_id
* основания (claims)
* решение пользователя
* последствия (post-hoc)

---

## 12. Что **не входит** в MVP (осознанно)

* Глобальный Project Context объект
* Active learning
* Полноценное планирование execution graph
* Автоматический rollback
* Формальная онтология проекта

Все эти пункты **совместимы с архитектурой**, но **не блокируют MVP**.

---

## 13. MVP Checklist (для команды)

* [ ] Индексация кода + docs
* [ ] RAG retrieval
* [ ] Understanding State (минимальный)
* [ ] Claims extraction (best-effort)
* [ ] Dialogue Memory
* [ ] Risk assessment
* [ ] Эскалация по правилам
* [ ] Lineage

---

## 14. Итог

**v3 — это:**

✅ архитектурно цельная система
✅ без преждевременной сложности
✅ с явным управлением неопределённостью
✅ реально реализуемая как MVP
✅ с понятным путём эволюции
