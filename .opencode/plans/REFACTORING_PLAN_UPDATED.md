# 📋 ОБНОВЛЕННЫЙ СБАЛАНСИРОВАННЫЙ ПЛАН РЕФАКТОРИНГА
## Дата обновления: 2026-02-28
---
### 📌 Важная информация
- Старый исходный план сохранен как `REFACTORING_PLAN_FINAL.md`
- Этот план сочетает два ключевых требования:
  1. Реализацию всего функционала из исходного плана
  2. 100% прохождение всех e2e-тестов сразу после завершения каждой фазы

---
## 🎯 Общие правила для всех фаз
1. После каждой фазы запускать только тесты, затрагиваемые реализованным функционалом
2. Если тесты не проходят — сразу исправлять ошибки, не переходить к следующей фазе
3. Фиксировать изменения в истории коммитов с прозрачными сообщениями
4. Сохранять обратную совместимость на каждом этапе

---
## Фаза 0: Базовая стабильность и критические исправления
**Цель из исходного плана**: Исправить критические баги, добавить разделяемые клиенты, повторные попытки запросов, ограничение скорости и корректное завершение работы.
1.  **Реализация функционала**:
    - Добавить асинхронный закрываемый HTTP-клиент в `embedding_model.py`
    - Добавить декораторы `@retry` для всех запросов к внешним сервисам
    - Добавить `asyncio.Semaphore` для ограничения скорости запросов к embedding-модели
    - Заменить голые `except` на конкретные исключения во всех стейджах
    - Добавить корректное завершение работы всех сервисов в `main.py`
2.  **Обновление тестов**:
    - Исправить фикстуры базовых тестов здоровья сервисов
    - Добавить тесты на устойчивость к временным сбоям внешних сервисов
3.  **Проверка**: Все базовые тесты стабильности и здоровья сервисов проходят.

---
## Фаза 1: Консолидация хранилища (единый источник истины)
**Цель из исходного плана**: Оставить только `chunks_vectors` как основное хранилище чанков, удалить отдельную таблицу `chunks`.
1.  **Реализация функционала**:
    - Удалить отдельную таблицу `chunks` и репозиторий `ChunkRepository`
    - Удалить двойную запись в `IndexingStage` (оставить только запись в векторное хранилище)
    - Реализовать методы работы с чанками через фильтрацию метаданных в PGVectorStore
    - Исправить метод удаления чанков по путям файлов
    - Настроить индексацию по метаданным `file_path` в векторном хранилище
2.  **Обновление тестов**:
    - Заменить все запросы к старой таблице `chunks` на запросы через векторное хранилище
    - Удалить тесты, зависящие от устаревшей таблицы
    - Обновить фикстуры для очистки базы данных под новую схему
3.  **Проверка**: Все тесты работы с хранилищем и индексацией файлов проходят.

---
## Фаза 2: Генерация саммари файлов и модулей
**Цель из исходного плана**: Добавить автоматическую генерацию саммари файлов и агрегацию их по модулям.
1.  **Реализация функционала**:
    - Добавить поле `last_summarized_at` в метаданные файлов
    - Реализовать стейдж генерации саммари файлов из чанков через LLM
    - Добавить стейдж агрегации саммари по директориям (модулям)
    - Реализовать обнаружение Python-модулей через наличие `__init__.py`
2.  **Обновление тестов**:
    - Добавить новые тесты для проверки генерации саммари файлов
    - Добавить тесты для проверки агрегации саммари по модулям
    - Обновить интеграционные тесты, проверяющий полный цикл индексации
3.  **Проверка**: Все тесты генерации саммари и полного цикла индексации проходят.

---
## Фаза 3: Центральнизированная конфигурация и устойчивость
**Цель из исходного плана**: Вынести все хардкодные параметры в конфигурацию, добавить повторные попытки для операций с базой данных.
1.  **Реализация функционала**:
    - Перенести все хардкодные параметры в класс `PipelineConfig`
    - Добавить загрузку конфигурации из переменных окружения
    - Добавить retry для всех операций с базой данных
    - Обновить конфигурацию для всех стейджев пайплайна
2.  **Обновление тестов**:
    - Добавить тесты на загрузку конфигурации из окружения
    - Обновить тесты на работу с параметрами конфигурации
    - Добавить тесты на устойчивость к сбоям базы данных
3.  **Проверка**: Все тесты конфигурации и устойчивости базы данных проходят.

---
## Фаза 4: Подготовка к графовой БД
**Цель из исходного плана**: Спроектировать архитектуры интеграции с графовой базой данных.
1.  **Реализация функционала**:
    - Описать схему графовой БД с вершинами и ребрами связей
    - Спроектировать архитектуру экспортера данных из векторного хранилища в графовую БД
    - Добавить абстрактный интерфейс для работы с графовой БД
2.  **Обновление тестов**:
    - Добавить тесты на проектирование схемы графовой БД
    - Обновить тесты на совместимость с будущими изменениями в хранилище
3.  **Проверка**: Все тесты проектирования графовой БД проходят.

---
## Фаза 5: Очистка устаревшего кода
**Цель из исходного плана**: Удалить неиспользуемые классы, упростить интерфейс хранилища.
1.  **Реализация функционала**:
    - Удалить неиспользуемые репозитории и адаптеры старой схемы
    - Упростить интерфейс базового хранилища, удалить лишние методы
    - Удалить неиспользуемый код из стейджев и сервисов
2.  **Обновление тестов**:
    - Удалить все устаревшие тесты, зависящие от удаленного кода
    - Обновить все оставшиеся тесты под упрощенный интерфейс хранилища
3.  **Проверка**: Все тесты на упрощенный интерфейс хранилища проходят.

---
## Фаза 6: Финальное тестирование и оптимизация
**Цель из исходного плана**: Добавить новые тесты, провести бенчмарки, довести покрытие тестов до более высокого уровня.
1.  **Реализация функционала**:
    - Добавить новые интеграционные тесты для всего пайплайна
    - Добавить бенчмарки производительности индексации и поиска
    - Оптимизировать производительность пайплайна на основе результатов бенчмарков
2.  **Обновление тестов**:
    - Запустить все e2e-тесты параллельно, исправить оставшиеся падающие тесты
    - Довести покрытие тестов до более высокого уровня (не менее 85%)
3.  **Проверка**: **100% прохождение всех e2e-тестов**, покрытие тестов не менее 85%.

---
## 📊 Итоговый результат
После завершения всех фаз вы получите:
1.  Полностью реализованный функционал из исходного плана рефакторинга
2.  100% прохождение всех e2e-тестов после каждой фазы
3.  Централизованную конфигурацию, стабильную работу сервиса и оптимизированную производительность